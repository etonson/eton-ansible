---
- name: Deploy global .gitconfig
  template:
    src: gitconfig.j2
    dest: "{{ ansible_user_dir }}/.gitconfig"
    mode: '0644'

- name: Deploy .gitconfig-ginmao
  template:
    src: gitconfig-ginmao.j2
    dest: "{{ ansible_user_dir }}/.gitconfig-ginmao"
    mode: '0644'
